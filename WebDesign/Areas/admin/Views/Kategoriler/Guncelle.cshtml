<a href="/admin/Kategoriler" class="btn btn-primary">Geri Git</a>

<!--Controller tarafına inputlara girilen verileri göndermemizi sağlayan bir mekanizmadır.-->
<form action="/Admin/Guncelle/@Model.id" method="post">
    <br />
    <input type="text" name="CategoryName" class="form-control" value="@Model.CategoryName" /><br /><br />
    <button type="submit" class="btn btn-primary">Güncelle</button>

</form>

@if(ViewBag.message != null)
{
    <div class="alert alert-info">@ViewBag.message</div>
}